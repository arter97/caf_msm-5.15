################################################################################
### Inheriting MSM configs
. ${KERNEL_DIR}/build.config.msm.sdxbaagha.common

################################################################################
## Variant setup
MSM_ARCH=sdxbaagha-iot

################################################################################

## Inheriting MSM configs
. ${KERNEL_DIR}/build.config.msm.common

DEFCONFIG="generic_csm_defconfig"
function build_defconfig_fragments() {
        if [[ "${VARIANT}" =~ ^(perf_defconfig|debug_defconfig)$ ]]; then
                if [ "${MSM_ARCH}" = sdxbaagha-iot ]; then
                        config_128m=${KERNEL_DIR}/arch/${ARCH}/configs/vendor/sdxbaagha-128m.config
                        [ -e "${config_128m}" ] && apply_defconfig_fragment ${config_128m} vendor/${MSM_ARCH}_defconfig
                fi
                apply_defconfig_fragment ${KERNEL_DIR}/arch/${ARCH}/configs/vendor/sdxbaagha.config vendor/${MSM_ARCH}_defconfig
                apply_defconfig_fragment ${KERNEL_DIR}/arch/${ARCH}/configs/vendor/${MSM_ARCH}.config vendor/${MSM_ARCH}_defconfig

                if [ "${VARIANT}" = perf_defconfig ]; then
                        return
                fi

                if [ "${MSM_ARCH}" = sdxbaagha-iot ]; then
                        config_128m_debug=${KERNEL_DIR}/arch/${ARCH}/configs/vendor/sdxbaagha-128m_debug.config
                        [ -e "${config_128m_debug}" ] && apply_defconfig_fragment ${config_128m_debug} vendor/${MSM_ARCH}_defconfig
                fi
                apply_defconfig_fragment ${KERNEL_DIR}/arch/${ARCH}/configs/vendor/sdxbaagha_debug.config vendor/${MSM_ARCH}_defconfig
                apply_defconfig_fragment ${KERNEL_DIR}/arch/${ARCH}/configs/vendor/${MSM_ARCH}_debug.config vendor/${MSM_ARCH}-debug_defconfig
        else
                echo "Variant '${VARIANT}' unsupported by mdm"
                exit 1
        fi
}

build_defconfig_fragments

