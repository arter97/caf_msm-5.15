################################################################################
## Inheriting configs from ACK
. ${ROOT_DIR}/common/build.config.common
. ${ROOT_DIR}/common/build.config.aarch64

################################################################################
## Variant setup
MSM_ARCH=sa525m_televm
VARIANTS=(defconfig debug_defconfig)
[ -z "${VARIANT}" ] && VARIANT=debug_defconfig

if [ -e "${ROOT_DIR}/msm-kernel" -a "${KERNEL_DIR}" = "common" ]; then
	KERNEL_DIR="msm-kernel"
fi

BOOT_IMAGE_HEADER_VERSION=3
PAGE_SIZE=4096
VM_DTB_IMG_CREATE=1
PREFERRED_USERSPACE=vm

VENDOR_RAMDISK_BINARY=${ROOT_DIR}/prebuilts/qcom_boot_artifacts/vm/sa525m_televm/sa525_televm-initrd.gz


declare -A MKDTIMGDTBS

################################################################################
## Define TELE VM parameters

TELEVM_KERNEL_OFFSET=0x0
TELEVM_DTB_OFFSET=0x2000000
TELEVM_RAMDISK_OFFSET=0x2100000

MKDTIMGDTBS["sa525m_televm"]="sa525m-televm"

################################################################################
## Define FOTA VM parameters
#FOTAVM_KERNEL_OFFSET=6FBE0000
#FOTAVM_DTB_OFFSET=0x71BE0000
#FOTAVM_RAMDISK_OFFSET=0x71CE0000
#FOTAVM_DUMMY_IMG_OFFSET=0x75FE0000

#MKDTIMGDTBS["sa525m_fotavm"]="sa525m-fotavm"

VM_SIZE_EXT4=270000000
DUMMY_IMG_SIZE=4096

################################################################################
## Inheriting MSM configs
. ${KERNEL_DIR}/build.config.msm.common
. ${KERNEL_DIR}/build.config.msm.vm
